# VideoControl MPV Client - Dependencies

# Socket.IO client for real-time communication
# Updated to 5.14.0 to fix CVE-2025-61765 (unsafe pickle deserialization)
python-socketio[client]==5.14.0

# HTTP requests
# Updated to 2.32.4 to fix CVE-2024-47081 (.netrc credential leakage)
requests==2.32.4

# Note: MPV binary should be installed via system package manager
# Ubuntu/Debian: sudo apt install mpv
# CentOS/RHEL: sudo yum install mpv
# Arch: sudo pacman -S mpv

